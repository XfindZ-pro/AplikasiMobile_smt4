name: aplikasi_mobile_smt4
description: Proyek Akhir Pemrograman Mobile Semester 4 - Migrated to Flutter
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  image_picker: ^1.0.7
  google_fonts: ^6.3.0
  flutter_spinkit: ^5.2.0
  intl: ^0.19.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
